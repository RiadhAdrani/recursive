import { describe, it, expect, jest, beforeEach } from "@jest/globals";
import { RecursiveApp } from "../../app";
import { RecursiveStateManager } from "../index";

describe("StateManager", () => {
  let stateManager: RecursiveStateManager;

  beforeEach(() => {
    //
  });

  it("should add item", () => {
    //
  });
});
