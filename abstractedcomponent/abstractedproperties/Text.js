export default function ({
     color,
     family,
     kerning,
     size,
     stretch,
     style,
     weight,
     lineHeight,
     letterSpacing,
     transform,
     alignment,
     decoration,
     justification,
     shadow,
     overflow,
     indentation,
     wordSpacing,
     wordWrap,
     writingMode,
     wordBreak,
}) {
     return { ...arguments[0] };
}
