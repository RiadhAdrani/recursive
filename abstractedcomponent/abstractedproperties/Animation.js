export default function ({
     steps,
     name,
     delay,
     direction,
     duration,
     fillMode,
     iteration,
     playState,
     timingFucntion,
}) {
     return { ...arguments[0] };
}
